//FUNCIONES

const saludar = function(nombre){
    return `Bienvenido ${nombre} a React`;
}

const saludarFlecha = (nombre) => {
    return `Bienvenido ${nombre} a React`;
}

const saludarSimple = (nombre) => `Bienvenido ${nombre} a React`;

console.log(saludarSimple('Alejandra Salazar'));

